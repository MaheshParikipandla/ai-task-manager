# PostgreSQL configuration
DB_URL=jdbc:postgresql://localhost:5432/aitaskmanager
DB_USERNAME=your_db_user
DB_PASSWORD=your_db_password

# AI integration (do NOT commit your real key)
# Set AI_API_KEY to enable real AI calls; leave empty to use the local mock fallback
AI_API_KEY=
AI_BASE_URL=https://api.example.com/v1
